import  logo  from "/logoStudio.png"

export const Header = () => {
    return (
        <header className="flex h-[20%] ">
            <img className="" src={logo} alt="" />
        </header>
    )
}