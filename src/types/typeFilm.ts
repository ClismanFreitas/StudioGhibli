export type Films = {
    description: string,
    director: string,
    id: string,
    image: string,
    movie_banner: string,
    producer: string,
    title: string,
    release_date: number,
    rt_score: number,
    running_time: number
}
